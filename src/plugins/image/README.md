# Image
 
Built-in Fizzy plugin. This plugin is responsible for intercepting documents that are registered images (.PNG, .JPEG, .JPG) and rendering them as an image viewer rather than allowing the Text plugin to render the plain text.

It offers no editing functionality, and only allows viewing images, panning and zooming. 